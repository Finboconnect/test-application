apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: test-application
  description: my test application.
  annotations:
    github.com/project-slug: Finboconnect/test-application
    # backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: user:default/iambrotherfranklyn  # or a Group — see below
  lifecycle: experimental