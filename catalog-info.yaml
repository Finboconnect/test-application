apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: test-app
  description: my test application.
  annotations:
    github.com/project-slug: Finboconnect/test-application
    backstage.io/kubernetes-id: kanban-dev
    backstage.io/kubernetes-namespace: dev
    backstage.io/kubernetes-label-selector: app=kanban-dev
    # backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: user:default/finboCloud  # or a Group — see below
  lifecycle: experimental